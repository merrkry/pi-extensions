# RTK

Rewrites supported non-interactive commands from Pi's `bash` tool and the bundled `exec_command` tool through [`rtk rewrite`](../../docs/compatibility.md#rtk-cli) to simplify CLI output and reduce token usage.

`exec_command` calls with `tty: true` are left unchanged. A command is rewritten only when RTK exits with code 0 or 3 and emits a non-empty command on stdout; all other outcomes pass through unchanged. Commands already beginning with `rtk ` and calls made with `RTK_DISABLED=1` also pass through.

The module requires `rtk >= 0.23.0` in `PATH`. It disables only itself when setup fails and otherwise fails open: cancellation, execution failures, malformed output, or unexpected errors never prevent the original shell command from running. Version checks and rewrites have a two-second bound and use typed Effect failures internally.

## Origin and license

This module was derived from the Pi extension generated by `rtk init -g --agent pi` and then modified for this repository. RTK is developed at [rtk-ai/rtk](https://github.com/rtk-ai/rtk) and licensed under the [Apache License 2.0](https://github.com/rtk-ai/rtk/blob/master/LICENSE).
