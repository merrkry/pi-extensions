# Better TUI Chrome

Keeps pi's normal editor behavior while tightening the surrounding chrome.

This extension intentionally only touches TUI mode.
Print/JSON/RPC sessions keep pi's default UI behavior.
